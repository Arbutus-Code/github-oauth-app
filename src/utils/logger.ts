/**
 * @file src/utils/logger.ts
 * This file is reserved for potential custom application-level logging utilities.
 * Currently, the application primarily uses Fastify's built-in logger, which is
 * configured in `src/index.ts`.
 *
 * If more advanced or centralized logging control is needed beyond what Fastify
 * provides out-of-the-box (e.g., specific log formatting, multiple transport streams
 * managed independently of Fastify's core logger), this file can be expanded.
 */

export {}; // Ensures this file is treated as a module.
